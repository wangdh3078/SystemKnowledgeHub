import { onBeforeUnmount, ref } from 'vue'
import { ApiError } from '../../../api/errors/ApiError'
import { integrationsApi } from '../api/integrationsApi'
import type { IntegrationContractField, IntegrationDetailResponse, IntegrationWriteInput } from '../api/integrationContracts'
import type { ActorContext } from '../../../app/stores/actor'
export function useIntegrationDetail(){const detail=ref<IntegrationDetailResponse|null>(null);const loading=ref(false);const error=ref<string|null>(null);const saving=ref(false);const conflict=ref(false);let controller:AbortController|null=null;async function load(id:number):Promise<boolean>{controller?.abort();controller=new AbortController();loading.value=!detail.value;error.value=null;try{detail.value=await integrationsApi.detail(id,controller.signal);conflict.value=false;return true}catch(caught:unknown){if(caught instanceof DOMException&&caught.name==='AbortError')return false;error.value=caught instanceof Error?caught.message:'集成关系加载失败。';return false}finally{loading.value=false}}async function saveOverview(input:IntegrationWriteInput):Promise<boolean>{if(!detail.value)return false;saving.value=true;try{await integrationsApi.updateOverview(detail.value.id,{...input,concurrencyToken:detail.value.concurrencyToken});return await load(detail.value.id)}catch(caught:unknown){conflict.value=caught instanceof ApiError&&caught.status===409;error.value=caught instanceof Error?caught.message:'集成关系保存失败。';return false}finally{saving.value=false}}async function saveFields(fields:readonly IntegrationContractField[],actor:ActorContext):Promise<boolean>{if(!detail.value)return false;saving.value=true;try{await integrationsApi.replaceContractFields(detail.value.id,{fields,actor,concurrencyToken:detail.value.concurrencyToken});return await load(detail.value.id)}catch(caught:unknown){error.value=caught instanceof Error?caught.message:'契约字段保存失败。';return false}finally{saving.value=false}}onBeforeUnmount(()=>controller?.abort());return{detail,loading,error,saving,conflict,load,saveOverview,saveFields}}
