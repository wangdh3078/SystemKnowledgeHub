# REPO-CLEAN-B01 — Safe Workspace Cleanup Plan

## Status

```text
PROPOSED — AWAITING HUMAN REPOSITORY CLEANUP GATE
```

This is an execution plan, not execution authorization. REPO-CLEAN-A01 did not perform any listed action. B01 may execute only explicitly approved CLEAN IDs and must never use `git clean -fd`, broad reset/restore, wildcard migration deletion, or recursive deletion against an unresolved path.

## Cleanup plan

| ID | Path | Current State | Classification | Proposed Action | Risk | Evidence | Reference Updates | Verification Required |
|---|---|---|---|---|---|---|---|---|
| CLEAN-001 | Entire current staged/modified/untracked formal delivery set | 13 R100 staged docs moves; 78 real content diffs; 74 untracked files, all classified as formal source/test/migration/doc/asset | KEEP_TRACKED | Before cleanup, human-review the phase boundaries and create one or more coherent Git commits/checkpoints containing the approved AUTH/UI/KC/REV work. Do not stage App_Data or ignored artifacts by force. | High | Full diff executed; source/import/project references verified; prior PASS reports exist; no deleted path; untracked manifest is formal | None beyond preserving current canonical paths | Re-run status/diff, .NET build and affected tests, frontend type-check/tests/build/lint, EF migration list/model check, then confirm expected commit contents |
| CLEAN-002 | `.github/workflows/` (and `.github/` if empty afterward) | Empty physical directory; untracked; no workflow file | SAFE_DELETE | Remove the empty directory only. | Low | Empty; not tracked; no references; no solution/project/Vite/tsconfig reference; not a symlink | None | Confirm exact resolved path, directory empty, then `Test-Path=false`; Git status unchanged |
| CLEAN-003 | `src/SystemKnowledgeHub.Api/Controllers/` | Empty template-era directory | SAFE_DELETE | Remove the empty directory. | Low | Empty; not tracked; no path references; actual controllers are under `Features/*/Api`; not included as a csproj Folder | None | Confirm empty and exact path; build not required for empty-dir removal; status unchanged |
| CLEAN-004 | `src/SystemKnowledgeHub.Web/.vscode/` | Empty local IDE directory; already ignored | SAFE_DELETE | Remove the empty directory. | Low | Empty; no references; `.gitignore` covers `.vscode/`; not a symlink | None | Confirm empty/exact path and absence afterward |
| CLEAN-005 | `src/SystemKnowledgeHub.Web/src/assets/` | Empty Vite-template directory | SAFE_DELETE | Remove the empty directory. | Low | Empty; no `src/assets` or `@/assets` imports; no config reference; not tracked/symlink | None | Confirm targeted reference search remains empty and path is absent |
| CLEAN-006 | `.vs/` | 20 ignored Visual Studio files, about 13.67 MiB | SAFE_DELETE | After closing Visual Studio, remove only the resolved repository `.vs` directory. | Low | Not tracked; `.gitignore` rule; generated IDE state; no build/runtime reference | None | Prove no VS process owns it; validate resolved target under repository; remove; confirm Git status unchanged |
| CLEAN-007 | `src/SystemKnowledgeHub.Api/{bin,obj}` and `tests/SystemKnowledgeHub.Api.Tests/{bin,obj}` | 630 ignored build files, about 127.6 MiB combined | SAFE_DELETE | Remove the four exact build-output directories. | Low | Not tracked; covered by `**/bin/` and `**/obj/`; SDK build regenerates them | None | Validate four exact paths; remove; run `dotnet build SystemKnowledgeHub.sln --no-restore` when dependencies remain available; status must not gain files |
| CLEAN-008 | `src/SystemKnowledgeHub.Web/dist/` | 5 ignored Vite build files, about 1.80 MiB | SAFE_DELETE | Remove the exact dist directory. | Low | Not tracked; covered by `**/dist/`; `npm run build` regenerates it | None | Validate exact path; remove; optionally run production build and confirm output remains ignored |
| CLEAN-009 | `src/SystemKnowledgeHub.Web/node_modules/` | 20,428 ignored dependency files, about 213.25 MiB | SAFE_DELETE | Optional disk-space cleanup only; remove exact directory when reinstall time/network/cache cost is acceptable. | Medium | Not tracked; covered by `**/node_modules/`; `package-lock.json` is canonical | None | Preserve package files; run `npm ci`, then type-check/test/build before development resumes |
| CLEAN-010 | `.gitignore` | Core outputs covered; SQLite variants, standalone Vite cache, Playwright/test-result dirs, DP key dirs, temp files/dirs, and Vite local env variants are uncovered | SAFE_GITIGNORE | Add narrow patterns for `*.sqlite`, `*.sqlite3` and companions, `**/.vite/`, `playwright-report/`, `test-results/`, conventional `DataProtection-Keys/`, root task temp dirs, `*.tmp`, `*.bak`, `.env.local`, and `.env.*.local`. Do not ignore source/docs/migrations or tracked mode env files. | Low | `git check-ignore --no-index` probes show each gap; current `.gitignore` content reviewed | Update `.gitignore` only | Probe every added pattern and negative-control official files; run `git check-ignore -v`; inspect status to ensure formal untracked files remain visible |
| CLEAN-011 | `docs/INDEX.md`, `docs/PROJECT_FILE_MAP.md`, `README.md` | No concise docs index; map stops short of complete KC-B02–REV-B01 inventory; README still says not to add KnowledgeDocument | KEEP_TRACKED | Add a concise categorized docs index and update current README/map navigation/baseline statements without rewriting frozen or historical documents. | Medium | 86 baseline docs across six sections; Project File Map is 73,690 bytes; current code/migrations/reports prove KnowledgeDocument/Revision are implemented | Add index links; update README and Project File Map references | Content review; local Markdown link/path audit; ensure frozen specs/history unchanged; `git diff --check` |
| CLEAN-012 | Root `design-qa.md`, `docs/reports/design-qa.md`, VS02–VS06 report references | Root file currently contains VS06, while VS02–VS05 also cite it as their QA record; stable docs report is VS01 | HUMAN_REVIEW | Decide per-slice canonical QA naming/provenance. If approved, preserve root content as a specifically named VS06 report and repair every historical reference honestly; do not claim lost VS02–VS05 content exists. | High | Content headings inspected; exact reference search shows conflicting semantic use; DOC-STRUCTURE-B01 intentionally left root file, so a move is not yet proven safe | Potentially Project File Map plus VS02–VS06 reports | Human content/provenance review; all path references resolve; no report result/history is fabricated |
| CLEAN-013 | `artifacts/` | 54 ignored images / 27.40 MiB; Bootstrap and VS01–VS06 documentation references them; VS05 directory has no direct path reference | HUMAN_REVIEW | Define evidence-retention policy: keep local, select immutable evidence for Git/LFS, or archive externally. Do not bulk delete. Classify files per slice before any removal. | Medium | Tracked reports/backticked paths refer to artifacts; ignored means clones cannot reproduce those links; sizes and reference counts audited | Reports/design QA if evidence location changes | Hash inventory, per-file reference check, visual evidence spot-check, updated links, and confirmation that required evidence is recoverable |
| CLEAN-014 | `src/SystemKnowledgeHub.Api/App_Data/system-knowledge-hub.db*` | Local/internal-pilot DB 507,904 B, SHM 32,768 B, zero-byte WAL; ignored; all 18 migrations applied | DO_NOT_TOUCH | Exclude from cleanup. If humans later request reset, first stop owners, checkpoint/backup all DB companions, validate backup, and use an explicit database lifecycle procedure. | High | appsettings/design-time factory/csproj reference App_Data; Development migrates/seeds; ignored DB can contain authored users/content; companions belong to WAL lifecycle | None | Backup hash and restore test, SQLite integrity/FK/history checks, explicit human approval; never delete companions independently |
| CLEAN-015 | `src/SystemKnowledgeHub.Api/Persistence/Migrations/` and `KnowledgeHubDbContextModelSnapshot.cs` | 18 recognized migrations; five applied migrations represented by eight untracked files; snapshot modified and current | DO_NOT_TOUCH | Commit the official migration files/snapshot under CLEAN-001; never delete/reorder/squash during workspace cleanup. | High | EF list recognizes all 18; actual DB history contains all 18; no pending model change; migration tests/reports reference later files | None | EF migration list, no-pending-model check, actual history/integrity, focused migration tests |
| CLEAN-016 | `docs/specifications/` and `product-design/final-ui/` | Frozen canonical specifications and 34 Golden assets | DO_NOT_TOUCH | No cleanup action. | High | AGENTS and Final UI Inventory explicitly freeze these paths | None | Confirm zero diff under both paths after B01 |
| CLEAN-017 | Non-canonical `product-design` source/archive/QA assets | 103 total files / 132.37 MiB; 33 duplicate hash groups; about 51.97 MiB duplicate-copy bytes | KEEP_HISTORY | Retain. Optionally document canonical-vs-history roles in docs index; do not deduplicate by hash. | High | Final UI Inventory requires canonical copies; freeze report says archive files remain; duplicates span source, final, and archive roles | Docs index only if added | Inventory validation: every Golden exists and hashes/paths are unchanged |
| CLEAN-018 | `src/SystemKnowledgeHub.Web/.env`, `.env.development`, `.env.production` | Three tracked, exact 23-byte duplicates; each sets `/api` | KEEP_TRACKED | Retain mode-specific files; protect only `.env.local` variants through CLEAN-010. | Low | Vite implicitly loads filenames by mode; content contains no secret; tiny storage cost | None | Build in normal mode; confirm tracked files remain visible and local variants are ignored |
| CLEAN-019 | `.git/objects` | 2,350 loose objects / 97.49 MiB; four temp garbage files / 3.03 MiB; many dangling objects; no packs | HUMAN_REVIEW | Do nothing until CLEAN-001 is committed and the repository is backed up. Then decide standard Git maintenance; never manually remove object files and never prune recovery objects without approval. | High | `git count-objects -vH`; connectivity-only fsck exit 0 with dangling objects; heavily dirty worktree raises recovery value | None | Backup/clone validation, clean status, full fsck, approved Git maintenance command, post-maintenance fsck and commit reachability |
| CLEAN-020 | Bootstrap diagnostic backend/frontend/test surface | Active authenticated endpoint, active `/foundation` route, bootstrap API/page, and tests; not primary navigation | HUMAN_REVIEW | Handle only in a separate product/security-approved change: retain, make Development-only, replace with formal health check, or remove with tests/routes/docs together. | Medium | Direct imports/routes/tests; Project File Map marks it potential review; SEC-A01 says Production should not expose the diagnostic surface | Router, controller/tests, map, security/verification docs depending on decision | Security review, build/tests, production-environment route check, documentation update |
| CLEAN-021 | Repository line endings and binary storage (`.gitattributes` / Git LFS) | Repeated CRLF conversion notices; no `.gitattributes`; 132.37 MiB tracked design images; Git LFS installed but unused | HUMAN_REVIEW | Decide policy after a clean checkpoint. Avoid mass normalization or history rewrite in cleanup. New binary additions may use LFS prospectively if approved. | High | Diff warnings; no attributes; large-file/hash audit; rewriting existing history would be disruptive | Potential new `.gitattributes` and contributor docs | Trial on separate branch/clone; verify zero unintended source churn, asset availability, CI/tool support, and repository clone |
| CLEAN-022 | `src/SystemKnowledgeHub.Api/appsettings.json` index/stat state | Git status reports modified, but HEAD/index/worktree hashes are identical and diff is empty | HUMAN_REVIEW | Do not edit. Allow a normal reviewed index refresh/checkpoint to clear the stat-only condition; investigate filesystem/Git config only if it persists. | Low | All three hashes equal `aa666883...`; raw/cached diff empty | None | Re-run status/hash/diff; confirm no content change |

## Recommended approved execution sequence

1. **Protection gate:** CLEAN-001, then explicitly exclude CLEAN-014/015/016/017 from cleanup.
2. **Empty directories:** CLEAN-002–005.
3. **Regenerable local output:** CLEAN-006–008; CLEAN-009 only if reinstall cost is accepted.
4. **Ignore hygiene:** CLEAN-010.
5. **Documentation navigation/current-state repair:** CLEAN-011.
6. **Separate human decisions:** CLEAN-012, CLEAN-013, CLEAN-020, CLEAN-021, CLEAN-022.
7. **Git object maintenance last:** CLEAN-019 only after commit, backup, and separate approval.

## B01 mandatory guardrails

- Resolve and validate every destructive target before acting.
- Execute only explicitly approved CLEAN IDs; report each result independently.
- Preserve user changes and unrelated dirty paths.
- Never use `git clean -fd`, `git reset --hard`, broad restore/checkout, migration deletion, or wildcard database deletion.
- Stop build/dev/test processes before deleting their exact generated outputs.
- Re-run `git status`, `git diff --stat`, and `git diff --check` after each logical group.
- Stop after the approved B01 cleanup and verification report; do not start REV-B02.
